{
  "name": "deskcheck",
  "version": "0.4.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "bin": {
    "deskcheck": "./cli/deskcheck.mjs"
  },
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "tsc --noEmit && vite build && cp -r icons dist/icons",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/chrome": "^0.0.304",
    "jsdom": "^24.1.3",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vite-plugin-web-extension": "^4.5.0",
    "vitest": "^4.1.2"
  },
  "dependencies": {
    "fflate": "^0.8.2",
    "lucide": "^1.11.0"
  }
}
